EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_KEY=your-publishable-or-anon-key
EXPO_PUBLIC_API_URL=https://thread-backend.thread-nucleus.workers.dev
# Local backend alternative: http://localhost:8787
#
# Password reset deep links: in Supabase Dashboard → Auth → URL configuration,
# allow redirect URLs for Linking.createURL('reset-password'), e.g.
#   thread://reset-password
#   thread://**
# Expo Go also needs the exp://…/--/reset-password URL from createURL on that device.
